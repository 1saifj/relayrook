interface:
  display_name: "RelayRook"
  short_description: "Delegate work across your installed coding agents"
  default_prompt: "Use $relayrook to delegate this task to an installed coding agent and verify the result."

policy:
  allow_implicit_invocation: true
